# 007 设备事务、备份与故障恢复核心

- 状态：实现中；本轮离线核心、正常与中断测试已完成，整项任务尚未验收。
- 日期：2026-09-22。
- 授权：用户已理解模块用途，授权自主完成无 iPod 的实现和正常测试；仅遇到与项目明确冲突的决定再询问。此前已说明故障注入范围，本任务一并验证。
- 规格：[SPEC.md](SPEC.md)；验证：[VALIDATION.md](VALIDATION.md)。
- 依据：DEC-SAFE-001 至 008、SAFETY_MODEL、蓝图 BP-FLOW-002/003、BP-INIT-*、BP-TXN-*。不新增第三方源码或声称官网证明本项目事务的正确性。

## 输入、动作、产物

输入为稳定身份/挂载代次/能力版本、数据库 profile、源文件与不可变计划。实现独立 Core、受目录约束的文件系统、恢复记录、备份保留与故障注入。所有运行只使用电脑测试目录及合成数据库。

007 验证正常导入/删除/初始化、取消、存储失败、进程中断后的恢复及恢复自身中断。后续 008 接入媒体/服务写入口并单独授权实机；当前 foobar 服务继续拒绝写入。

005 尚有 DUP-001；不伪造其已验收。用户此次授权使 007 的离线核心可先行；真实写入前必须解决设备身份歧义及 005 的相关门槛。

## 步骤与通过标准

1. 冻结下列核心规格，不改变已批准的产品决定。
2. 编写真实数据库正常与故障测试，再实现核心。
3. x64 Debug/Release 全构建与回归；包内容审计。
4. 提供电脑侧验证报告，人工应用加载及真实介质行为保持待验证。用户明确确认前不标记已验收。

本任务不启动应用、不连接或写入设备，不修改 Ref 或 FooCrate，不提交或推送。

## 2026-09-22 离线阶段结果

已新增永久 transaction 静态库、Windows 目录适配器、真实传统数据库验证适配器、不可变计划、流式音频复制、提交/恢复记录、取消、快照仓库、显式 LKG 标记与基线清单比较器。任务 003/004 的 Reader/Writer 继续复用；未移植参考程序的旧事务顺序。

Debug/Release 全构建和各 13/13 CTest 通过，包括 238 个提交前/后中断点、970 次恢复中断检查。本轮正常目录 fixture 已保留，详见 VALIDATION。

这里的完成范围是用户本次要求的无 iPod 实现与测试。整个 007 继续保持“实现中”：尚未接入运行中只读服务的恢复记录发现器，没有终端用户恢复入口，没有进行组件包交付或人工验收；这些不能用静态库和测试通过替代。首次实机基线的完整树枚举、外部目录选择、稳定身份和任务授权必须在接入时完整验证，当前清单比较器不能单独签发真实设备写入许可。005 的 DUP-001 仍必须在真实写入前解决。

本轮未改变组件版本或生成新组件包：事务库由独立 Core 测试消费，已安装组件仍只有既有只读服务。后续涉及组件交付时再生成新的 prerelease，旧 beta.3/beta.4 文件保留。

## 提交授权

2026-09-22：用户在本轮测试结果交接后明确要求 commit + push，授权提交并推送本轮源码、测试和文档。既有 beta.3/beta.4 包不纳入提交；不创建 Release，不改变任务验收状态或实机写入权限。
